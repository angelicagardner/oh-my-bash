# Override
alias gb="git --no-pager branch"

# Additional
alias k='kubectl'
alias kubeconfig-service-cluster='export KUBECONFIG=/path/to/kubeconfig; kubectl config set-context --current --namespace=ns'
